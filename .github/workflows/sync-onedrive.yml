name: Sync Excel from OneDrive

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get Microsoft Graph token
        env:
          CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
        run: |
          set -e

          echo "Requesting Microsoft Graph token..."

          RESPONSE=$(curl -sS -X POST \
            "https://login.microsoftonline.com/${TENANT_ID}/oauth2/v2.0/token" \
            -H "Content-Type: application/x-www-form-urlencoded" \
            --data-urlencode "client_id=${CLIENT_ID}" \
            --data-urlencode "client_secret=${CLIENT_SECRET}" \
            --data-urlencode "scope=https://graph.microsoft.com/.default" \
            --data-urlencode "grant_type=client_credentials")

          TOKEN=$(python -c "import json,sys; print(json.load(sys.stdin)['access_token'])" <<< "$RESPONSE")

          if [ -z "$TOKEN" ]; then
            echo "Failed to obtain Microsoft Graph token."
            exit 1
          fi

          echo "TOKEN=$TOKEN" >> "$GITHUB_ENV"

      - name: Download Excel from OneDrive
        run: |
          set -e

          echo "Downloading Site_Verification_PowerBI_Ready.xlsx..."

          curl -L --fail --retry 3 \
            -H "Authorization: Bearer ${TOKEN}" \
           "https://graph.microsoft.com/v1.0/users/azzam.abdullah%40pttmg.com/drive/root:/Zona%2013/Asset%20Register%20%26%20Verifikasi/Dashboard/Site_Verification_PowerBI_Ready.xlsx:/content" \

          echo "Download completed."

          ls -lh source.xlsx

      - name: Install Python dependency
        run: |
          python -m pip install --upgrade pip
          pip install openpyxl

      - name: Generate dashboard data
        run: |
          python sync_excel.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data.js

          if git diff --cached --quiet; then
            echo "No changes detected."
          else
            git commit -m "Update dashboard data"
            git push
          fi
